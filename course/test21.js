// Библиотека для тестирования кода 

assert(kvadrat(2) == 4);

// Выведет нам, что все хорошо, если квадрат числа будет равен 4

assert(kvadrat(6) !== 4); 

// Выведет нам, что тоже все хорошо, если значение 6 в квадрате (36) не будет равно 4

assert.equal(kvadrat(2), 4);

// Выведет нам, что все хорошо, если квадрат числа будет равен 4. Первое значение ...(2) это вводимые данные, .., 4 - то чему оно должно быть равно 
