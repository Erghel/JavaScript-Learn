// Обычная функция
const penSize = (yoursize) => {
  return 1.5 * yoursize;
};

console.log(penSize(x)); // Вместо X ставим размер вашей ручки! 
                         // Формула по размеру ручки выдумана! 
